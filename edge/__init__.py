"""CADENCE edge package: the browser SDK, injector, and proxy addon.

Installed as a package (not just package data) so `cadence proxy` can
resolve a real filesystem path for mitmproxy's -s flag in both a
checkout and a pip-installed environment.
"""
